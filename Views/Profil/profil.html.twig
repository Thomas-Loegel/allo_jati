{% extends "template.html.twig" %}

{% block body %}
<div class="container-fluid d-flex justify-content-start">
   <div class="row">
      <aside class="col-12 col-md-12 p-0 bg-dark">
         <div class="text-center text-white bg-dark font-weight-bold">GESTION PROFILE</div>
         <div class="text-center text-white bg-dark font-weight-bold">{{user}}</div>
         <nav class="navbar navbar-expand navbar-dark bg-dark flex-md-column flex-row align-items-start">
            <div class="collapse navbar-collapse">
               <ul class="flex-md-column flex-row navbar-nav w-100 justify-content-between">

                  <li class="nav-item">Connexion
                     <ul>
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/Profile/ModifierPseudo_{{user}}">Pseudo</a></li>


                        {% set avatar = 'avatar' %}
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/Profile/ModifierAvatar_{{avatar}}">Avatar</a></li>

                        {% set mdp = 'mdp' %}
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/Profile/ModifierMdp_{{mdp}}">Mot de passe </a></li>
                     </ul>
                  </li>
                  <li class="nav-item">Messagerie
                     <ul>
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/">Envoyer</a></li>
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/">Réception</a></li>
                     </ul>
                  </li>

                  {#************************ANTHONY************************#}
                  <li class="nav-item">Suppresion
                     <ul>
                        <li><a class="nav-link pl-0" href="{{baseUrl}}/Profile/ModifierCompte_">Compte</a></li>
                     </ul>


                     {# <form name="search" method="POST" action="{{baseUrl}}/Admin/Utilisateur">
                        <div class="form-row">
                           <div class="col-md-12 mb-3">
                              <input type="text" class="form-control" name="pseudo" placeholder="Pseudo">
                           </div>
                        </div>
                        <div class="row justify-content-center">
                           <button class="btn btn-secondary" type="submit">Rechercher</button>
                        </div>
                     </form>

                     <form name="idMovie" method="POST" action="{{baseUrl}}/Admin/Utilisateur">
                        <div class="form-row">
                           <div class="col-md-12 mb-3">
                              <input type="text" class="form-control" name="idMovie" placeholder="Nom Film">
                           </div>
                        </div>
                        <div class="row justify-content-center">
                           <button class="btn btn-secondary" type="submit">Rechercher</button>
                        </div>
                     </form>#}
                  </li>
                  {#******************************************************#}
               </ul>
            </div>
         </nav>
      </aside>
   </div>
   <div class="col d-flex justify-content-center pt-5 pb-5">
   
      {% if (slug == 'pseudo') %}

      <form name="pseudo" method="POST" action="{{baseUrl}}/">
         <div class="form-row">
            <div class="col-md-12 mb-3">
               <label for="pseudo">Changer votre Pseudonyme</label>
               <input type="text" class="form-control" name="pseudo" placeholder="{{user}}">
            </div>
         </div>
         <div class="row justify-content-center">
            <button class="btn btn-secondary" type="submit">Modifier</button>
         </div>
      </form>
      {% endif %}


      {% if (slug == 'avatar') %}

      <form name="avatar" method="POST" action="{{baseUrl}}/">
         <div class="form-row">
            <div class="col-md-12 mb-3">
               <label for="avatar">Changer votre Avatar</label>
               <input type="text" class="form-control" name="avatar" placeholder="http://">
            </div>
         </div>
         <div class="row justify-content-center">
            <button class="btn btn-secondary" type="submit">Modifier</button>
         </div>
      </form>
      {% endif %}

      {% if (slug == 'mdp') %}

      <form name="mdp" method="POST" action="{{baseUrl}}/">
         <div class="form-row">
            <div class="col-md-12 mb-3">
               <label for="mdp">Changer votre Mot de Passe</label>
               <input type="text" class="form-control" name="mdp" placeholder="Nouveau mot de passe">
            </div>
         </div>
         <div class="row justify-content-center">
            <button class="btn btn-secondary" type="submit">Modifier</button>
         </div>
      </form>
      {% endif %}





      {% if (slug == 'compte') %}

      <form name="compte" method="POST" action="{{baseUrl}}/">
         <div class="form-row">
         </div>
         <div class="row justify-content-center">
            <button class="btn btn-secondary" type="submit">Suppression</button>
         </div>
      </form>
      {% endif %}
   </div>

   {% endblock %}