<!DOCTYPE html>
<html lang="fr">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta http-equiv="X-UA-Compatible" content="ie=edge">
   <link rel='icon' href='https://i.ibb.co/RSYk2Kw/jatilogo.png' type='image/x-icon'/ >
   <title>{% block title %}Allo Jati{% endblock %}</title>

   {% block stylesheets %}
   <link rel="stylesheet" href="{{baseUrl}}/assets/css/style.css">
   <link rel="stylesheet" href="{{baseUrl}}/assets/css/bootstrap+reboot.css">

   {% endblock %}
</head>

<body>
   {% block header %}
   <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light shadow border-bottom border-secondary mb-5 p-0">
         <a class="navbar-brand ml-5" href="#">
            <img src="https://i.ibb.co/RSYk2Kw/jatilogo.png" width="60" height="60" class="d-inline-block align-top" alt="">
         </a>
         <div class="collapse navbar-collapse " id="navbarText">
            <ul class="navbar-nav mx-auto font-weight-bold p-0">
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 m-0 py-4 " href="{{baseUrl}}">HOME</a>
               </li>
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 py-0 m-0 py-4" href="{{baseUrl}}/Artistes">ARTISTES</a>
               </li>
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 py-0 m-0 py-4" href="{{baseUrl}}/Films">FILMS</a>
               </li>
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 py-0 m-0 py-4" href="{{baseUrl}}/Connexion">CONNEXION</a>
               </li>
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 py-0 m-0 py-4" href="{{baseUrl}}/Deconnexion">DECONNEXION</a>
               </li>
               <li class="nav-item shadow-drop-2-bottom active">
                  <a class="nav-link px-4 py-0 m-0 py-4" href="{{baseUrl}}/Admin">ADMIN</a>
               </li>
            </ul>
         </div>
      </nav>
   </header> 
   {% endblock %}

   {% block body %}
   <main>
      {# Here the main content, you can create other blocks if there are useful to you #}
   </main>
   {% endblock %}

   {% block footer %}
   {# Footer ici #}
   <footer>

   </footer>
   {% endblock %}

   {% block javascripts %}
   <script src="{{baseUrl}}/assets/js/app.js"></script>
   {# Here you can add only the JS that you need #}
   {% endblock %}
</body>
</html>
